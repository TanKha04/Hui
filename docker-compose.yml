version: '3.8'

services:
  tracuuhoi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tracuuhoi_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/app/data
